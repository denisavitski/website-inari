<e-zones class="visually-hidden zones"></e-zones>

<script>
  export class ZonesElement extends HTMLElement {
    protected connectedCallback() {}

    protected disconnectedCallback() {}
  }

  if (!customElements.get('e-zones')) {
    customElements.define('e-zones', ZonesElement)
  }

  declare global {
    interface HTMLElementTagNameMap {
      'e-zones': ZonesElement
    }
  }
</script>
